import { PlaceholderScreen } from '../../../components/v2/PlaceholderScreen';

export default function AntesEDepoisPage() {
  return <PlaceholderScreen title="Antes e depois" note="Comparação da sua produção ao longo do tempo — construído numa próxima etapa." />;
}
