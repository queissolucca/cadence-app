import { PlaceholderScreen } from '../../../components/v2/PlaceholderScreen';

export default function PontosFracosPage() {
  return <PlaceholderScreen title="Pontos fracos" note="Treino dirigido aos seus erros mais frequentes — construído numa próxima etapa." />;
}
