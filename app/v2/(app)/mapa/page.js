import { SectionHead, Card } from '../../../../components/ui';

export default function MapaPageV2() {
  return (
    <>
      <SectionHead title="Mapa" />
      <Card>
        <p style={{ margin: 0, color: 'var(--ink-soft)', fontSize: 14 }}>Mapa de cenários e progresso — construído numa próxima etapa.</p>
      </Card>
    </>
  );
}
